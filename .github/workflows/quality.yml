name: "Legacy quality and security analysis"

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read
  security-events: write

concurrency:
  group: legacy-greed-quality
  cancel-in-progress: true

jobs:
  codeql:
    uses: Steffo99/.github/.github/workflows/quality-python-codeql.yml@main
    permissions:
      actions: read
      contents: read
      security-events: write
